{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.0.1"
  },
  "scripts": {
    "dev": "lerna run dev",
    "dev:client": "lerna run dev --scope=client",
    "dev:server": "lerna run dev --scope=server"
  }
}
